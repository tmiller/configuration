# Homebrew bash completion
if [ -f `brew --prefix`/etc/bash_completion ] ; then
  . `brew --prefix`/etc/bash_completion
fi

export PATH="${HOME}/android-sdk/platform-tools:${HOME}/bin:/usr/texbin:${PATH}"
